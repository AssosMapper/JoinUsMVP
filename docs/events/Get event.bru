meta {
  name: Get event
  type: http
  seq: 1
}

get {
  url: {{url}}/events/:id
  body: none
  auth: none
}

params:path {
  id: 
}

tests {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
}
