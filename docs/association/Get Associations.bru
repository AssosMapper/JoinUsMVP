meta {
  name: Get Associations
  type: http
  seq: 2
}

get {
  url: {{url}}/associations?position=actuelle&around=25
  body: none
  auth: none
}

params:query {
  position: actuelle
  around: 25
}

tests {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
}
